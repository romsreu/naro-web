# Base URL of the auth service, routed through the API gateway
# (used by app/actions/auth.ts and app/api/auth/refresh/route.ts).
# Defaults to http://localhost:8443/auth-service/api/auth when unset.
AUTH_SERVICE_URL=http://localhost:8443/auth-service/api/auth
